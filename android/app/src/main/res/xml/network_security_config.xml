<?xml version="1.0" encoding="utf-8"?>
<!--
  v26 上线合规：
  - 默认禁止明文 HTTP（cleartextTrafficPermitted=false）
  - WebView/HTTP 客户端只能访问 HTTPS
  - 仅 debug 构建允许 10.0.2.2/localhost 用于本地联调
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
